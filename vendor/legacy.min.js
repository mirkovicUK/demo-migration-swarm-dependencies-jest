/* vendor/legacy.min.js — vendored, minified third-party bundle.
 * EXCLUSION FIXTURE (Req 7.1/7.2/7.3): this file lives under `vendor/` AND
 * matches the `*.min.js` glob, so the analyzer must ignore it entirely. Its
 * import of the (undeclared, fake) package `jquery-legacy` must NEVER appear
 * in the generated package.json dependencies, and `vendor` must never appear
 * in the tsconfig `include`. If either leaks, exclusion is broken. */
import $ from "jquery-legacy";
export const noop=()=>$;
